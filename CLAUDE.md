## 基本规则
不允许读取下面文件的内容，以免造成泄密
- .venv

## 功能测试约定


- 原始需求的路径： `./原始文档/电商系统需求规格说明书.md`
- 拆分需求的输出路径: `./需求内容/目录.md`
- 用例设计的输出路径: `./用例设计`
- 用例评审的输出路径: `./用例评审`
- 最终用例的输出路径: `./最终用例`

对于输出路径，如果不存在则自动创建

## 功能测试设计 Skill 流程

本项目的功能测试设计流程由以下 Skills 组成，默认按顺序执行：

1. `/test-design-01-requirement-split`
   - 输入：归一化后的需求 Markdown
   - 输出：`./需求内容/` 下的模块化需求文档、风险清单和目录索引

2. `/test-design-02-functional-test-design`
   - 输入：`./需求内容/`
   - 输出：`./用例设计/` 下的需求理解、测试点和测试用例

3. `/test-design-03-test-case-review`
   - 输入：`./需求内容/` 和 `./用例设计/`
   - 输出：`./用例评审/评审报告.md`

4. `/test-design-04-excel-export`
   - 输入：`./用例设计/` 和 `./用例评审/`
   - 输出：`./最终用例/功能测试用例设计.xlsx`

执行规则：

- 当用户要求执行整个流程时，不要跳过中间步骤
- 如果上一步产物不存在，不要继续下一步
- 如果上一步质量检查失败，先修复上一步产物
- 需求不明确时标记为“待确认”，不要自行补充规则。
- Excel 导出必须在用例评审之后执行
- 如用户只要求执行某一步，先检查该步骤依赖的上游产物是否存在