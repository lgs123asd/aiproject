---
name: 拆分需求文档
description: 收到新的需求文档时，使用本技能进行归一化和拆分，输出便于 Agent 读取和理解的md需求文档。或者当用户要求拆分、整理、规范化、模块化需求文档准备测试设计时，请使用此技能。
---

你现在是一名测试分析负责人，需要把原始需求文档转换成 Markdown：这是设计测试用例的前置步骤，必须完成这个任务才能继续设计测试用例。

## 输入变量
本SKILL的使用依赖以下变量，如何用户尚未提供，需要先请用户补充，然后再正式执行

- 原始需求的路径
- 拆分需求的输出路径



## 任务流程

### 1. 将原始需求文档转为md格式

首先检查原始需求文档的路径、大小、格式。
如果原始需求文档不是txt、md等文本格式，使用 skill `mineru-document-extractor` 将其转为 md 格式
注意：
1. 环境变量中有 token，使用 MinerU 的`extract`模式而非`flash-extract`模式
2. 处理并保存在目录 `{原始需求的路径}` 中（即原路径）




### 2. 拆分文档内容

按业务领域拆分需求，不要只按原章节机械切割

你可以先参考下面的通用结构，再根据实际文档调整：

| 文件 | 适用内容 |
|---|---|
| `01-项目概述.md` | 项目背景、角色、术语、范围、总体流程、非功能需求 |
| `02-身份与权限.md` | 注册、登录、认证、授权、账号状态、角色权限 |
| `03-信息查询与展示.md` | 首页、列表、搜索、筛选、详情、信息展示 |
| `04-核心业务流程.md` | 创建、提交、确认、办理、支付、审批等主流程 |
| `05-状态管理与后续处理.md` | 状态流转、撤销、取消、审核、申诉、复核、关闭 |
| `06-个人中心与数据维护.md` | 用户资料、地址、联系人、偏好设置、历史记录 |
| `07-管理配置.md` | 管理后台、系统配置、基础数据、权限管理、运营配置 |
| `08-风险清单与待确认汇总.md` | 全部风险、待确认项、冲突点、缺失规则 |
| `目录.md` | 文件索引、模块说明、原文来源、规则编号索引 |

如果文档不适合这个结构，请给出你自己的拆分方案，并说明原因。

拆分需求的输出路径: `{拆分需求的输出路径}`（即输出目录和文件名与输入文件一致，但文件格式为md）

### 3. 聚合业务内容

把分散在不同章节、但属于同一功能模块的规则聚合到同一个文件
保留原文中的规则编号、表格、流程图、状态、字段、验收标准和待确认问题。

每个模块文件的建议结构

```markdown
# {模块名称}

> 来源：{原文章节号或标题}

## 1. 模块范围

说明该模块包含哪些功能，不包含哪些功能。

## 2. 业务流程

保留原文中的流程图、流程步骤或状态流转。没有流程时写“原文未提供明确流程”。

## 3. 功能规则

| 规则编号 | 规则内容 | 来源 |
|---|---|---|

## 4. 字段、状态和配置

整理表单字段、查询条件、状态枚举、配置项、角色权限等测试设计需要的信息。

## 5. 异常、边界和验收标准

提取原文中已经明确的异常处理、边界条件和验收标准。

## 6. 风险小结

| 风险编号 | 风险类型 | 风险描述 | 来源 | 对测试设计的影响 |
|---|---|---|---|---|
```

### 4. 识别内容风险

识别影响测试设计的风险，例如未定义、歧义、冲突、缺少边界、缺少状态说明，

将风险内容补充到拆分文档的 `6. 风险小结` 中

常见的风险类型有：

| 风险类型 | 判断标准 |
|---|---|
| 待确认 | 原文明确写了“待确认”“TBD”“？”等 |
| 未定义 | 功能被提到，但缺少字段、规则、流程、状态或异常处理 |
| 有歧义 | 描述可能有多种理解，例如“按系统规则”“适当提示”“默认处理” |
| 前后冲突 | 不同章节对同一规则、状态、字段或权限的描述不一致 |
| 测试阻塞 | 缺少账号、测试数据、配置说明、状态枚举，导致用例无法确定预期 |



## 输出要求

1. 先输出“拆分方案摘要”，说明你将生成哪些文件。
2. 然后在 `{拆分需求目录}` 下创建对应 Markdown 文件。
3. 不要修改原始需求文件。
4. 不要补充文档外的业务规则。
5. 需求不明确时，只标记为风险或待确认，不要替业务方下结论。
6. 最后输出“完成清单”：文件名、对应原文章节、主要内容、风险数量。